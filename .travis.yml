language: java
jdk:
    - openjdk12

cache:
    directories:
        - .git/lfs
git:
    lfs_skip_smudge: true

install:
    - git lfs pull
git diff
before_install:
    grep -v '^#' assets/src/main/resources/META-INF/services/bisq.asset.Asset | sort --check --dictionary-order --ignore-case

after_failure:
    - pwd
    - cat /home/travis/build/bisq-network/bisq/core/build/reports/tests/test/classes/bisq.core.api.model.PaymentAccountFormTest.html
