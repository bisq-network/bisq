# Deny-list fixture used only by apitest/docker/run-e2e-tests.sh policy phases.
# Normal BTC_REGTEST runs do not load this resource unless --denyListResource points here.

bannedCurrencies=XMR
bannedPaymentMethods=F2F
