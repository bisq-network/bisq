dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../gradle/libs.versions.toml"))
        }
    }
}

include 'commons'
include 'gradle-tasks'
include 'packaging'
include 'tor-binary'
